#!/bin/sh
set -eu

make fmt

changed_files="$(git diff --name-only -- '*.go')"
if [ -n "$changed_files" ]; then
	echo "pre-commit: formatting changed these Go files:"
	printf '%s\n' "$changed_files"
	echo "pre-commit: review the diff and re-run commit"
	exit 1
fi
